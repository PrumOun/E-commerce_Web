// src/components/Preloader.js
import React from 'react';

const Preloader = () => (
  <section>
    <div id="preloder">
      <div className="loader"></div>
    </div>
  </section>

);

export default Preloader;
